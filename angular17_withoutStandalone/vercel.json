{
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/angular17-without-standalone"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/angular17-without-standalone/$1"
    }
  ]
}
